//! Module holding the `Clone` and `Copy` traits

@lang("copy_trait")
/// Currently does nothing.
pub trait Copy {}

@lang("clone_trait")
/// Gives the ability for the user to Clone the type.
pub trait Clone {
    fn clone(self: &Self) -> Self;
}
