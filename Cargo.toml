workspace = { members = ["mira"] }
[package]
name = "mirac"
version = "0.1.0"
edition = "2021"

authors = ["Skylar (github.com/FishingHacks)"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
parking_lot = "0.12.3"
mira = { path = "./mira" }
clap = { version = "4.5.28", features = ["derive"] }
