macro! meow {
    ($first:ident) => { $first };
    ($first:ident $(, $ident:ident)* $(,)?) => {
        concat_idents!($first, $(` `, $ident),*)
    };
}

//meow!(a, b)

pub fn main() {
    "Hello, World!".println();
}
